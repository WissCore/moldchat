# apps/

Client applications. Each subdirectory is a platform-specific client; they share the underlying protocol but use platform-native UI and crypto bindings.

- [`android/`](android/) — Android client (Kotlin + Jetpack Compose)
- [`ios/`](ios/) — iOS client (Swift + SwiftUI)
