# Android client

Kotlin + Jetpack Compose. Uses `libsignal-android` for end-to-end cryptography, SQLCipher for local storage, and the Android Keystore for key wrapping.

Build: `./gradlew assembleRelease`
