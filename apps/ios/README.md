# iOS client

Swift + SwiftUI. Uses `LibSignalClient` for end-to-end cryptography, SQLCipher for local storage, and the iOS Keychain for key wrapping.

Build: `xcodebuild ...` (full instructions in [docs/build.md](../../docs/build.md), TBD).
