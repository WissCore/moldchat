# RFCs

The RFC process is required for cross-cutting changes: cryptographic protocol modifications, threat-model changes, new external dependencies in security-sensitive paths.

Process: open a PR adding a new file `NNNN-kebab-title.md` to this directory. Discuss in the PR. On Accepted, the file is merged with the assigned number; implementation can begin.
